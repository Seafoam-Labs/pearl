/* Translation-only shim: the opaque type declaration from GLib 2.88.3 gmain.h.
 * Full GLib ABI comes from the pinned Ghostty bindings. No functions or code. */
typedef struct _GMainContext            GMainContext;
