//! Stable action names shared by keyboard, overflow and context menus.
pub const Command = enum {
    back,
    forward,
    up,
    home,
    location,
    search,
    refresh,
    grid,
    list,
    split,
    switch_pane,
    details,
    hidden,
    new_tab,
    close_tab,
    new_window,
    light,
    compact,
    native,
    new_folder,
    rename,
    trash,
    copy,
    paste,
    operations,
    properties,
    open,
    open_tab,
    open_window,
    open_with,
    terminal,
    cut,
    paste_into,
    copy_to,
    move_to,
    copy_other,
    move_other,
    duplicate,
    link,
    delete,
    new_document,
    restore,
    restore_to,
    empty_trash,
    undo,
    redo,
    containing,
    follow_link,
    copy_location,
    bookmark_add,
    bookmark_rename,
    bookmark_remove,
    tab_duplicate,
    tab_left,
    tab_right,
    tab_other,
    tab_close_others,
    mount,
    unmount,
    eject,
    sort_name,
    sort_size,
    sort_type,
    sort_modified,
    reverse,
    folders_first,
    pin,
    favorite,
    menu_settings,
    advanced,
    allow_delete,
    archive,
    extract,
    bulk_rename,
    admin,
};
pub fn fileAction(c: Command) bool {
    return switch (c) {
        .new_folder, .rename, .trash, .copy, .paste, .properties, .open, .open_tab, .open_window, .open_with, .terminal, .cut, .paste_into, .copy_to, .move_to, .copy_other, .move_other, .duplicate, .link, .delete, .new_document, .restore, .restore_to, .empty_trash, .undo, .redo, .containing, .follow_link, .copy_location, .bookmark_add, .bookmark_rename, .bookmark_remove, .tab_duplicate, .tab_left, .tab_right, .tab_other, .tab_close_others, .mount, .unmount, .eject, .pin, .favorite, .menu_settings, .archive, .extract, .bulk_rename, .admin => true,
        else => false,
    };
}
