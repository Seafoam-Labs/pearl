# Pearl's private greetd configuration; /etc/greetd/config.toml is preserved.
[terminal]
vt = 7
switch = true

[general]
source_profile = false

[default_session]
command = "/usr/lib/pearl/pearl-greeter-host"
user = "pearl-greeter"
