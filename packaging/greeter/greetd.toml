# Installed as /etc/greetd/config.toml; the previous config is saved as config.toml.bak.
[terminal]
vt = 7
switch = true

[general]
source_profile = false

[default_session]
command = "/usr/lib/pearl/pearl-greeter-host"
user = "pearl-greeter"
